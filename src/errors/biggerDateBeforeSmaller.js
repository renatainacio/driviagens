export function biggerDateBeforeSmaller() {
    return {
        type: "biggerDateBeforeSmaller",
        message: "O parâmetro 'bigger-date' deve possuir uma data posterior ao parâmetro 'smaller-date'"
    }
}