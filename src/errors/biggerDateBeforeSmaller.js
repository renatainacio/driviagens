export function biggerDateBeforeSmaller() {
    return {
        type: "biggerDateBeforeSmaller",
        message: "The 'bigger-date' parameter must have a date that is later than the 'smaller-date' parameter."
    }
}